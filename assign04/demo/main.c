#include<stdio.h>
#include"mymath.h"
#include"test.h"

int main()
{
int n1,base,index;
test();
printf("enter the num:");
scanf("%d",&n1);
printf("the fact of given num is :%d\n",factorial(n1));
printf("the pow is : %d\n"mpower(base,index));  
return 0;
}

